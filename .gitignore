node_modules/
.env
.env.*
uploads/*
!uploads/.gitkeep

# logs
*.log

# scrape reports
data/scrape-report-*.json

# scrape preview files (validar antes de merge)
data/quotes-*-preview.json

# ignore-list do lint (curadoria local via painel)
data/lint-ignore.json

# painel web — local-only (curadoria privada, não expor no GitHub)
panel/

# Claude Code
.claude/

# VS Code local settings (mantém tasks e launch, ignora só o local)
.vscode/settings.json
